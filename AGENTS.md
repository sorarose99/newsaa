# دليل التطوير - تطبيق الصلاة على النبي

## الأوامر المهمة

### تثبيت المكتبات
```bash
flutter pub get
```

### تشغيل التطبيق
```bash
flutter run
```

### فحص الأخطاء
```bash
flutter analyze
```

### بناء التطبيق
```bash
# Android
flutter build apk --release

# iOS
flutter build ios --release
```

### تشغيل الاختبارات
```bash
flutter test
```

### تنظيف المشروع
```bash
flutter clean
```

## البنية المعمارية

### Clean Architecture
المشروع يتبع مبادئ Clean Architecture مع التقسيم التالي:

- **Core**: الخدمات والثوابت المشتركة
- **Features**: الميزات الرئيسية (Home, Counter, Statistics, etc.)
- **Data**: نماذج البيانات والمستودعات
- **Domain**: الكيانات وحالات الاستخدام
- **Presentation**: الواجهات والـ Providers

### State Management
- يستخدم المشروع **Provider** لإدارة الحالة
- كل ميزة لها Provider خاص بها

### قاعدة البيانات المحلية
- **Hive** للتخزين المحلي السريع
- ثلاثة Boxes رئيسية:
  - `salawat_box`: لحفظ عداد الصلوات
  - `settings_box`: للإعدادات
  - `achievements_box`: للإنجازات

## القواعد والاصطلاحات

### التسمية
- الملفات: `snake_case` مثل `salawat_counter_provider.dart`
- الكلاسات: `PascalCase` مثل `SalawatCounterProvider`
- المتغيرات والدوال: `camelCase` مثل `incrementCounter`

### اللغات
- الكود: بالإنجليزية
- التعليقات المهمة: بالعربية أو الإنجليزية
- الواجهات: بالعربية افتراضياً

### الألوان الرئيسية
```dart
primaryGreen: Color(0xFF00695C)
secondaryGold: Color(0xFFD4A574)
accentColor: Color(0xFF4CAF50)
```

### الخطوط
- الخط الأساسي: **Cairo** من Google Fonts
- مناسب للعربية والإنجليزية

## الميزات المنفذة

### ✅ تم الانتهاء
1. نظام العداد الأساسي
2. التذكيرات المحلية
3. الإحصائيات والرسوم البيانية
4. نظام الإنجازات
5. الوضع المظلم/الفاتح
6. قاعدة البيانات المحلية
7. تعدد اللغات (عربي/إنجليزي)
8. التحديات الجماعية
9. مكتبة الأصوات والتحكم بالصوت
10. جميع الشاشات الرئيسية (5 شاشات)

### 🔜 المراحل القادمة
1. تكامل Firebase (للمزامنة السحابية)
2. إضافة أصوات حقيقية
3. تكامل الساعات الذكية
4. دعم لغات إضافية

## ملاحظات مهمة للتطوير

### Firebase (المرحلة القادمة)
1. إنشاء مشروع Firebase
2. إضافة ملفات التكوين:
   - `google-services.json` لـ Android
   - `GoogleService-Info.plist` لـ iOS
3. تفعيل Authentication & Firestore

### التذكيرات
- تستخدم `flutter_local_notifications`
- تعمل بدون إنترنت
- يجب طلب الأذونات على Android 13+

### الأذونات المطلوبة

#### Android (android/app/src/main/AndroidManifest.xml)
```xml
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
<uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.VIBRATE"/>
```

#### iOS (ios/Runner/Info.plist)
```xml
<key>UIBackgroundModes</key>
<array>
    <string>remote-notification</string>
</array>
```

## الاختبار

### اختبار التذكيرات
1. تفعيل التذكيرات من الإعدادات
2. تحديد فترة قصيرة (مثل 1 دقيقة)
3. إغلاق التطبيق
4. انتظار الإشعار

### اختبار الإحصائيات
1. إضافة صلوات في أيام مختلفة
2. التحقق من الرسم البياني الأسبوعي
3. التأكد من دقة الأرقام

## التحسينات المستقبلية

### الأداء
- استخدام `const` للـ Widgets الثابتة
- تحسين عمليات قراءة/كتابة Hive
- Lazy loading للبيانات الكبيرة

### تجربة المستخدم
- إضافة Animations للتفاعلات
- تحسين رسائل الأخطاء
- إضافة Haptic Feedback

### الأمان
- تشفير البيانات الحساسة
- حماية Firebase Rules
- التحقق من المدخلات

## الموارد

- [Flutter Docs](https://docs.flutter.dev/)
- [Provider Package](https://pub.dev/packages/provider)
- [Hive Database](https://pub.dev/packages/hive)
- [Firebase Flutter](https://firebase.flutter.dev/)

## ملاحظات الإصدار

### v1.0.0 (الحالية)
- إصدار أولي بالميزات الأساسية
- دعم Android و iOS
- عمل محلي بالكامل

---

للمزيد من المعلومات، راجع [README.md](README.md)
