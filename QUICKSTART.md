# 🚀 دليل البدء السريع - تطبيق الصلاة على النبي

## التشغيل السريع

### 1. تثبيت المكتبات
```bash
flutter pub get
```

### 2. تشغيل التطبيق

#### على الويب (Chrome):
```bash
flutter run -d chrome
```

#### على Android:
```bash
flutter run -d android
```

#### على iOS:
```bash
flutter run -d ios
```

### 3. بناء التطبيق

#### بناء للويب:
```bash
flutter build web --release
```

#### بناء لـ Android:
```bash
flutter build apk --release
```

#### بناء لـ iOS:
```bash
flutter build ios --release
```

## 📱 اختبار الميزات

### العداد
1. افتح التطبيق
2. اضغط على الزر الكبير الأخضر
3. شاهد العداد يزيد
4. تحقق من عداد اليوم

### التذكيرات
1. انتقل إلى الإعدادات
2. فعّل التذكيرات
3. حدد فترة قصيرة (مثل 1 دقيقة)
4. اخرج من التطبيق
5. انتظر الإشعار

### الإحصائيات
1. انتقل إلى تبويب الإحصائيات
2. شاهد الرسم البياني الأسبوعي
3. تحقق من الأرقام

### الإنجازات
1. انتقل إلى تبويب الإنجازات
2. شاهد قائمة الإنجازات
3. أضف المزيد من الصلوات للوصول للإنجاز التالي

### المجموعات
1. انتقل إلى تبويب المجموعات
2. شاهد التحديات الجماعية
3. اضغط على + لإنشاء تحدي جديد

### مكتبة الأصوات
1. اذهب إلى الإعدادات
2. اضغط على "مكتبة الأصوات"
3. اختر صوت
4. اضبط مستوى الصوت

### تعدد اللغات
1. اذهب إلى الإعدادات
2. قسم "اللغة"
3. اختر بين العربية والإنجليزية
4. شاهد التطبيق يتحول فوراً

### الوضع المظلم
1. اذهب إلى الإعدادات
2. فعّل "الوضع المظلم"
3. شاهد التطبيق يتحول للثيم الداكن

## 🔧 حل المشاكل الشائعة

### المشكلة: فشل بناء Android
**الحل:**
```bash
flutter clean
flutter pub get
flutter build apk
```

### المشكلة: التذكيرات لا تعمل
**التحقق:**
- تأكد من منح أذونات الإشعارات
- تحقق من تفعيل التذكيرات في الإعدادات
- على Android 13+: تأكد من السماح بالإشعارات

### المشكلة: البيانات لا تحفظ
**السبب:** قد لا تكون Hive مهيأة بشكل صحيح
**الحل:** تأكد من تشغيل التطبيق بالكامل مرة واحدة

## 📊 بيانات الاختبار

التطبيق يأتي مع بيانات تجريبية للمجموعات:
- تحدي المليون صلاة (450,000 / 1,000,000)
- تحدي الأسبوع (75,000 / 100,000)

## 🎯 الأهداف السريعة للاختبار

1. **الوصول لأول إنجاز:** اضغط 10 مرات
2. **رؤية الإحصائيات:** أضف صلوات في أيام مختلفة
3. **اختبار التذكيرات:** حدد فترة دقيقة واحدة

## 💡 نصائح مفيدة

- **التطوير السريع:** استخدم Hot Reload مع `flutter run`
- **فحص الأخطاء:** استخدم `flutter analyze`
- **الاختبارات:** استخدم `flutter test`
- **التنظيف:** استخدم `flutter clean` عند حدوث مشاكل

## 📱 منصات مدعومة

- ✅ Android (API 21+)
- ✅ iOS (iOS 12+)
- ✅ Web (Chrome, Edge, Safari)
- ⏳ Windows (قريباً)
- ⏳ macOS (قريباً)
- ⏳ Linux (قريباً)

## 🔑 متطلبات النظام

### للتطوير:
- Flutter SDK 3.9.2+
- Dart 3.0+
- Android Studio / VS Code
- Xcode (لـ iOS/macOS)

### للمستخدم النهائي:
- Android 5.0+ (API 21)
- iOS 12.0+
- متصفح حديث (للويب)

---

**جاهز للتشغيل!** 🚀

استخدم `flutter run` وابدأ التطوير فوراً!
